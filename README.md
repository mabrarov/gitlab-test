# GitLab playground

Docker Compose project to run [GitLab](https://docs.gitlab.com/).

## Start

```shell
docker compose up -d --wait
```

## Stop

```shell
docker compose stop -t 600
```

## Removal

```shell
docker compose down -v -t 0
```
